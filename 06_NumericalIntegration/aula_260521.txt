objetivo da aula de hoje
diferencas finitas para uma viga bi-apoiada com carga distribuida

w = 2500 N/m
L = 6,0 m
I = 17,0 um
E = 200,0 GPa

eq de governo:
d2y/dx2 = M(x)/EI
condicoes de contorno:
y(0) = 0
y(6) = 0
x E [0;6]

resolver modelo de 2 formas:
usar o método analitico e usar o computador como calculadora
usar solucoes numericas --> necessita discretizacao do problema

para produzir um paper:
1. problema - motivacao
2. modelo a utilizar
3. metodologia para resolucao do problema
4. resultados
5. conclusoes

obter a expressao para M(x) == voltando ao problema da viga
M(x) = wx(L-x)/2
usando dif centradas:
(yi-1 + 2yi + yi+1)/h² = wxi(L-xi)/2
xi = hi		i E N, i <= n

REFERENCIAR A IMPLEMENTACAO PARA OS CODIGOS --> COMO MELHORAR OS RELATORIOS

solucao de sistema linear
matriz tridiagonal - algoritmo de thomas
-- openblas.net
USAR A BIBLIOTECA

problema de calculo numerico --> avaliar a diferenca entre precisoes simples e dupla
avaliar erros, sempre que possivel >>>> comparar precisao simples e dupla

diferencas finitas -- teoricamente diminui conforme o numero de pontos
problema e combinar isso com metodos diretos para diferencas finitas, pois o erro pode acumular

salvando parametros como ponteiros => evitando armazenamento inutil e generalizando o codigo

criar uma rotina dentro do programa principal, melhorando encapsulamento

comando associate == equacao visualizado de forma facilitada
armazena um apelido da funcao como ponteiro, esse apelido nao precisa ser declarado nas variaveis

comando LAPACK
gtsv = solver para matriz generica tridiagonal

desenvolver animacao com gnuplot
criar script .sh
#!/bin/bash

sed ==> programa do linux
sed -i '6d' (ultima linha do arquivo input)
echo $1 >>  (ultima linha do arquivo input)

escape character

barra vertical | funciona como pipe

./beam.exe dadosdeentrada dadosdesaida
gnuplot plot.plt
chmod +x *.sh >>> tornar o shell script executavel